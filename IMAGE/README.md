Put real job photos in this folder.

Example:

- `IMAGE/cuadro-electrico-melilla.jpg`
- `IMAGE/reparacion-fuga-bano.jpg`

Then add each photo entry to `works-data.js` using the path `IMAGE/file-name.jpg`.
